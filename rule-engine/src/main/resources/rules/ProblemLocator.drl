package rules

import com.example.ruleengine.ProblemContext

rule "Sample Rule"
when
    $context: ProblemContext(log contains "interface UP", command contains "status=UP")
then
    $context.setConclusion("Rule fired successfully!");
end